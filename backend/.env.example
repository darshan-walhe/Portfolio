PORT=
JWT_SECRET=
# Fixed Firebase Auth identity the backend uses to talk to Firestore.
# This MUST match a real user in Firebase Authentication, and MUST match
# the email checked by isAdmin() in your Firestore Security Rules.
ADMIN_EMAIL=
ADMIN_PASSWORD=
apiKey=
authDomain=
databaseURL=
projectId=
storageBucket=
messagingSenderId=
appId=
measurementId=